import { AlertTriangle } from 'lucide-react'
import { Card, EmptyState, PageHeader } from '@/components/admin/ui'
import { AuditRepository } from '@/lib/repositories/operations'
export const dynamic='force-dynamic'
export default async function Audit(){let items:Awaited<ReturnType<AuditRepository['list']>>=[];let error:string|null=null;try{items=await new AuditRepository().list()}catch(e){error=e instanceof Error?e.message:'Audit events could not be loaded'}return <div><PageHeader eyebrow="Governance" title="Audit log" description="Immutable operational mutation history from the Firestore audit_events collection."/>{error&&<div className="notice danger"><AlertTriangle size={17}/>{error}</div>}{!error&&items.length===0?<Card><EmptyState title="No audit events" description="No operational audit events have been recorded."/></Card>:<Card><div className="table-scroll"><table className="data-table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th><th>Reason</th></tr></thead><tbody>{items.map(e=><tr key={e.id}><td>{e.createdAt?.toLocaleString('en-MY')??'—'}</td><td>{e.actorAdminId}<small>{e.actorRole}</small></td><td>{e.action}</td><td>{e.entityType}: {e.entityId}</td><td>{e.reason??'—'}</td></tr>)}</tbody></table></div></Card>}</div>}
