import { AlertTriangle, LockKeyhole } from 'lucide-react'
import { Card, EmptyState, PageHeader, StatusBadge } from '@/components/admin/ui'
import { OperatorRepository } from '@/lib/repositories/operations'
export const dynamic='force-dynamic'
export default async function Operators(){let items:Awaited<ReturnType<OperatorRepository['listPending']>>=[];let error:string|null=null;try{items=await new OperatorRepository().listPending()}catch(e){error=e instanceof Error?e.message:'Operator applications could not be loaded'}return <div><PageHeader eyebrow="Network governance" title="Pending operator approvals" description="Applications are read from Firestore. Approval remains unavailable until the canonical schema and audited mutation contract are confirmed."/>{error&&<div className="notice danger"><AlertTriangle size={17}/>{error}</div>}{!error&&items.length===0?<Card><EmptyState title="No pending operator applications" description="Firestore returned no pending operator records."/></Card>:<Card><div className="table-scroll"><table className="data-table"><thead><tr><th>Operator</th><th>Contact</th><th>Documents</th><th>Vehicles</th><th>Submitted</th><th>Status</th><th>Action</th></tr></thead><tbody>{items.map(o=><tr key={o.id}><td><strong>{o.name??'Name unavailable'}</strong><small>{o.companyName??'Company unavailable'}</small></td><td>{o.email??'—'}<small>{o.phone??'—'}</small></td><td>{o.documents.length}</td><td>{o.vehicles.length}</td><td>{o.submittedAt?.toLocaleString('en-MY')??'—'}</td><td><StatusBadge tone="warning">{o.status}</StatusBadge></td><td><span title="An audited approval migration and validation contract are required"><LockKeyhole size={15}/> Review action unavailable</span></td></tr>)}</tbody></table></div></Card>}</div>}
